
# URL de conexão ao banco de dados PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/academia

# Nome de usuário do banco de dados
spring.datasource.username=postgres

# Senha do banco de dados
spring.datasource.password=Pedrinho10

# Configuração do Driver (opcional, o Spring Boot detecta automaticamente)
spring.datasource.driver-class-name=org.postgresql.Driver

# Configurações do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect